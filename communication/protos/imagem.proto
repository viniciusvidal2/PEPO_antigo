syntax = "proto3";

// Namespace para usar no Qt
package ImagemMsgProto;

// Mensagem da imagem total a ser enviada
message Imagem{
        string name = 1;
        int32 height = 2;
        int32 width = 3;
	// Mensagem de cada pixel com seu rgb
        message Pixel{
                int32 r = 1;
                int32 g = 2;
                int32 b = 3;
                int32 u = 4;
                int32 v = 5;
	}
        repeated Pixel pixels = 4;
}
