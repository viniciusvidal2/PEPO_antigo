syntax = "proto3";

// Namespace para usar no Qt
package NuvemMsgProto;

// Mensagem da imagem total a ser enviada
message Nuvem{
        string name = 1;
        int32 size = 2;
        // Mensagem de cada ponto com seu xyz_rgb
	message Ponto{
                double x = 1;
                double y = 2;
                double z = 3;
                int32 r = 4;
                int32 g = 5;
                int32 b = 6;
	}
        repeated Ponto pontos = 3;
}
